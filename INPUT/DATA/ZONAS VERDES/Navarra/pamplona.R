library(devtools)
library(greenR)
library(dplyr)
datospamplona<-get_osm_data("Pamplona,Comarca de PPamplona,Navarra,España")
indice_verde_pamplona<-calculate_green_index(datospamplona,4326,100)
str(indice_verde_pamplona)
typeof(indice_verde_pamplona)
dtpamplona<-as.data.frame(indice_verde_pamplona)
pamplona<-summarise(.data=dtpamplona,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(pamplona)
str(pamplona)