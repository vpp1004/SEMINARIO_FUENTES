library(devtools)
library(greenR)
library(dplyr)
datossantander<-get_osm_data("Santander,Cantabria,España")
indice_verde_santander<-calculate_green_index(datossantander,4326,100)
str(indice_verde_santander)
typeof(indice_verde_santander)
dtsantander<-as.data.frame(indice_verde_santander)
santander<-summarise(.data=dtlsantander,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(santander)
str(santander)