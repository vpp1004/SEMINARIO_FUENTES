library(devtools)
library(greenR)
library(dplyr)
datosalcañiz<-get_osm_data("Alcañiz,Bajo Aragón,Teruel,Aragón ,España")
indice_verde_alcañiz<-calculate_green_index(datosalcañiz,4326,100)
str(indice_verde_alcañiz)
typeof(indice_verde_alcañiz)
dtalcañiz<-as.data.frame(indice_verde_alcañiz)
alcañiz<-summarise(.data=dtalcañiz,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(alcañiz)
str(alcañiz)