library(devtools)
library(greenR)
library(dplyr)
datosutebo<-get_osm_data("Utebo,Zaragoza,Aragón,España")
indice_verde_utebo<-calculate_green_index(datosutebo,4326,100)
str(indice_verde_utebo)
typeof(indice_verde_utebo)
dtutebo<-as.data.frame(indice_verde_utebo)
utebo<-summarise(.data=dtlutebo,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(utebo)
str(utebo)