library(devtools)
library(greenR)
library(dplyr)
datosalicante<-get_osm_data("Alicante,Comunidad Valenciana,España")
indice_verde_alicante<-calculate_green_index(datosalicante,4326,100)
str(indice_verde_alicante)
typeof(indice_verde_alicante)
dtalicante<-as.data.frame(indice_verde_alicante)
alicante<-summarise(.data=dtalicante,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(alicante)
str(alicante)