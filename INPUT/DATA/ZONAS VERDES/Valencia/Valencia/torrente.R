library(devtools)
library(greenR)
library(dplyr)
datostorrente<-get_osm_data("Torrente,Huerta Sur,Valencia,Comunidad Valenciana,España")
indice_verde_torrente<-calculate_green_index(datostorrente,4326,100)
str(indice_verde_torrente)
typeof(indice_verde_torrente)
dttorrente<-as.data.frame(indice_verde_torrente)
torrente<-summarise(.data=dttorrente,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(torrente)
str(torrente)