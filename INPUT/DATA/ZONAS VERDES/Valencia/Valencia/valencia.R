library(devtools)
library(greenR)
library(dplyr)
datosvalencia<-get_osm_data("Valencia,Comunidad Valenciana,España")
indice_verde_valencia<-calculate_green_index(datosvalencia,4326,100)
str(indice_verde_valencia)
typeof(indice_verde_valencia)
dtvalencia<-as.data.frame(indice_verde_valencia)
valencia<-summarise(.data=dtvalencia,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(valencia)
str(valencia)