library(devtools)
library(greenR)
library(dplyr)
datoscastellon<-get_osm_data("Castellón,Comunidad Valenciana,España")
indice_verde_castellon<-calculate_green_index(datoscastellon,4326,100)
str(indice_verde_castellon)
typeof(indice_verde_castellon)
dtcastellon<-as.data.frame(indice_verde_castellon)
castellon<-summarise(.data=dtcastellon,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(castellon)
str(castellon)