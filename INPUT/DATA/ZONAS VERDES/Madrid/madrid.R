library(devtools)
library(greenR)
library(dplyr)
datosmadrid<-get_osm_data("Madrid,Comunidad de Madrid,España")
indice_verde_madrid<-calculate_green_index(datosmadrid,4326,100)
str(indice_verde_madrid)
typeof(indice_verde_madrid)
dtmadrid<-as.data.frame(indice_verde_madrid)
madrid<-summarise(.data=dtmadrid,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(madrid)
str(madrid)