library(devtools)
library(greenR)
library(dplyr)
datosgerona<-get_osm_data("Gerona,Cataluña,España")
indice_verde_gerona<-calculate_green_index(datosgerona,4326,100)
str(indice_verde_gerona)
typeof(indice_verde_gerona)
dtgerona<-as.data.frame(indice_verde_gerona)
gerona<-summarise(.data=dtgerona,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(gerona)
str(gerona)