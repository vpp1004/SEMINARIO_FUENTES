library(devtools)
library(greenR)
library(dplyr)
datoscordoba<-get_osm_data("Córdoba,Andalucía,España")
indice_verde_cordoba<-calculate_green_index(datoscordoba,4326,100)
str(indice_verde_cordoba)
typeof(indice_verde_cordoba)
dtcordoba<-as.data.frame(indice_verde_cordoba)
cordoba<-summarise(.data=dtcordoba,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(cordoba)
str(cordoba)