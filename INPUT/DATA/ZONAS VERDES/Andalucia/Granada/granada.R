library(devtools)
library(greenR)
library(dplyr)
datosgranada<-get_osm_data("Granada,Andalucía,España")
indice_verde_granada<-calculate_green_index(datosgranada,4326,100)
str(indice_verde_granada)
typeof(indice_verde_granada)
dtgranada<-as.data.frame(indice_verde_granada)
granada<-summarise(.data=dtgranada,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(granada)
str(granada)