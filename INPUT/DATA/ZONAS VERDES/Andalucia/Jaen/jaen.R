library(devtools)
library(greenR)
library(dplyr)
datosjaen<-get_osm_data("Jaén,Andalucía,España")
indice_verde_jaen<-calculate_green_index(datosjaen,4326,100)
str(indice_verde_jaen)
typeof(indice_verde_jaen)
dtjaen<-as.data.frame(indice_verde_jaen)
jaen<-summarise(.data=dtjaen,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(jaen)
str(jaen)