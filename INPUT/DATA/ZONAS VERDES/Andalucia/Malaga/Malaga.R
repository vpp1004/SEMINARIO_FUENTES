library(devtools)
library(greenR)
library(dplyr)
datosmalaga<-get_osm_data("Málaga,Andalucía,España")
indice_verde_malaga<-calculate_green_index(datosmalagaM,4326,100)
str(indice_verde_malaga)
typeof(indice_verde_malaga)
dtmalaga<-as.data.frame(indice_verde_malaga)
malaga<-summarise(.data=dtmalaga,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(malaga)
str(malaga)