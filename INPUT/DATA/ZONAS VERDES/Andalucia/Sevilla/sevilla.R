library(devtools)
library(greenR)
library(dplyr)
datossevilla<-get_osm_data("Sevilla,Andalucía,España")
indice_verde_sevilla<-calculate_green_index(datossevilla,4326,100)
str(indice_verde_sevilla)
typeof(indice_verde_sevilla)
dtsevilla<-as.data.frame(indice_verde_sevilla)
sevilla<-summarise(.data=dtsevilla,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(sevilla)
str(sevilla)