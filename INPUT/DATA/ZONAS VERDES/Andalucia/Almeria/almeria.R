library(devtools)
library(greenR)
library(dplyr)
datosalmeria<-get_osm_data("Almería,Andalucía,España")
indice_verde_almeria<-calculate_green_index(datosalmeria,4326,100)
str(indice_verde_almeria)
typeof(indice_verde_almeria)
dtalmeria<-as.data.frame(indice_verde_almeria)
almeria<-summarise(.data=dtalmeria,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(almeria)
str(almeria)