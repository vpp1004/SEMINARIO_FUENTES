library(devtools)
library(greenR)
library(dplyr)
datoscadiz<-get_osm_data("Cádiz, Andalucía,España")
indice_verde_cadiz<-calculate_green_index(datoscadiz,4326,100)
str(indice_verde_cadiz)
typeof(indice_verde_cadiz)
dtcadiz<-as.data.frame(indice_verde_cadiz)
cadiz<-summarise(.data=dtcadiz,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(cadiz)
str(cadiz)