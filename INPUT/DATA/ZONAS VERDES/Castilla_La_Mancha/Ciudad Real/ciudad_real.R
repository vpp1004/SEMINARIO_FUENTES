library(devtools)
library(greenR)
library(dplyr)
datosciudad_real<-get_osm_data("Ciudad Real,Castilla-La Mancha,España")
indice_verde_ciudad_real<-calculate_green_index(datosciudad_real,4326,100)
str(indice_verde_ciudad_real)
typeof(indice_verde_ciudad_real)
dtciudad_real<-as.data.frame(indice_verde_ciudad_real)
ciudad_real<-summarise(.data=dtciudad_real,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(ciudad_real)
str(ciudad_real)