library(devtools)
library(greenR)
library(dplyr)
datosmontalvos<-get_osm_data("Montalvos,Albacete,Castilla-La Mancha,España")
indice_verde_montalvos<-calculate_green_index(datosmontalvos,4326,100)
str(indice_verde_montalvos)
typeof(indice_verde_montalvos)
dtmontalvos<-as.data.frame(indice_verde_montalvos)
montalvos<-summarise(.data=dtmontalvos,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(montalvos)
str(montalvos)