library(devtools)
library(greenR)
library(dplyr)
datosalbacete<-get_osm_data("Albacete,Castilla-La Mancha,España")
indice_verde_albacete<-calculate_green_index(datosalbacete,4326,100)
str(indice_verde_albacete)
typeof(indice_verde_albacete)
dtalbacete<-as.data.frame(indice_verde_albacete)
albacete<-summarise(.data=dtalbacete,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(albacete)
str(albacete)