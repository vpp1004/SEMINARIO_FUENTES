library(devtools)
library(greenR)
library(dplyr)
datostoledo<-get_osm_data("Toledo,Castilla-La Mancha,España")
indice_verde_toledo<-calculate_green_index(datostoledo,4326,100)
str(indice_verde_toledo)
typeof(indice_verde_toledo)
dttoledo<-as.data.frame(indice_verde_toledo)
toledo<-summarise(.data=dttoledo,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(toledo)
str(toledo)