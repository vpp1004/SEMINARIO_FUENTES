library(devtools)
library(greenR)
library(dplyr)
datossegovia<-get_osm_data(("Segovia,Castilla y León,España"))
indice_verde_segovia<-calculate_green_index(datossegovia,4326,100)
str(indice_verde_segovia)
dtsegovia<-as.data.frame(indice_verde_segovia)
segovia<-summarise(.data=dtsegovia,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(segovia)