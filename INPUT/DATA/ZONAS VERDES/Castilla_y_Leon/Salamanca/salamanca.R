library(devtools)
library(greenR)
library(dplyr)
datossalamanca<-get_osm_data(("Salamanca,Castilla y León,España"))
indice_verde_salamanca<-calculate_green_index(datossalamanca,4326,100)
str(indice_verde_salamanca)
dtsalamanca<-as.data.frame(indice_verde_salamanca)
salamanca<-summarise(.data=dtsalamanca,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(salamanca)