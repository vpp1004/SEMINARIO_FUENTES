library(devtools)
library(greenR)
library(dplyr)
datosleon<-get_osm_data(("León,Castilla y León,España"))
indice_verde_leon<-calculate_green_index(datosleon,4326,100)
str(indice_verde_leon)
dtleon<-as.data.frame(indice_verde_leon)
leon<-summarise(.data=dtleon,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(leon)