library(devtools)
library(greenR)
library(dplyr)
datospalencia<-get_osm_data(("Palencia,Castilla y León,España"))
indice_verde_palencia<-calculate_green_index(datosmpalencia,4326,100)
str(indice_verde_palencia)
dtpalencia<-as.data.frame(indice_verde_palencia)
palencia<-summarise(.data=dtpalencia,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(palencia)