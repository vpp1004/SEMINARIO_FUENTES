library(devtools)
library(greenR)
library(dplyr)
datoszamora<-get_osm_data(("Zamora,Castilla y León,España"))
indice_verde_zamora<-calculate_green_index(datoszamora,4326,100)
str(indice_verde_zamora)
dtzamora<-as.data.frame(indice_verde_zamora)
zamora<-summarise(.data=dtzamora,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(zamora)