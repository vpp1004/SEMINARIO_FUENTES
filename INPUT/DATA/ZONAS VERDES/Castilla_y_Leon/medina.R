library(devtools)
library(greenR)
library(dplyr)
datosmedina<-get_osm_data(("Medina del Campo ,Castilla y León,47400,España"))
indice_verde_medina<-calculate_green_index(datosmedina4326,100)
str(indice_verde_medina)
dtmedina<-as.data.frame(indice_verde_medina)
medina<-summarise(.data=dtmedina,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(medina)