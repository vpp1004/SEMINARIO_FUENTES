library(devtools)
library(greenR)
library(dplyr)
datosavila<-get_osm_data(("Ávila,Castilla y León,España"))
indice_verde_avila<-calculate_green_index(datosavila,4326,100)
str(indice_verde_avila)
dtavila<-as.data.frame(indice_verde_avila)
avila<-summarise(.data=dtavila,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(avila)