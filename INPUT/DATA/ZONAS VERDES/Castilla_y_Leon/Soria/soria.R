library(devtools)
library(greenR)
library(dplyr)
datossoria<-get_osm_data("Soria,Castilla y León,España")
indice_verde_soria<-calculate_green_index(datossoria,4326,100)
str(indice_verde_soria)
typeof(indice_verde_soria)
dtsoria<-as.data.frame(indice_verde_soria)
soria<-summarise(.data=dtsoria,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(soria)
str(soria)