library(devtools)
library(greenR)
library(dplyr)
datosorense<-get_osm_data("Orense,Galicia,España")
indice_verde_orense<-calculate_green_index(datosorense,4326,100)
str(indice_verde_orense)
typeof(indice_verde_orense)
dtorense<-as.data.frame(indice_verde_orense)
orense<-summarise(.data=dtorense,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(orense)
str(orense)