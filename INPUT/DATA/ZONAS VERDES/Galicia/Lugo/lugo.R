library(devtools)
library(greenR)
library(dplyr)
datoslugo<-get_osm_data("Lugo,Galicia,España")
indice_verde_lugo<-calculate_green_index(datoslugo,4326,100)
str(indice_verde_lugo)
typeof(indice_verde_lugo)
dtlugo<-as.data.frame(indice_verde_lugo)
lugo<-summarise(.data=dtlugo,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(lugo)
str(lugo)