library(devtools)
library(greenR)
library(dplyr)
datossantiago<-get_osm_data("Santiago de Compostela,Santiago,La Coruña,Galicia,España")
indice_verde_santiago<-calculate_green_index(datossantiago,4326,100)
str(indice_verde_santiago)
typeof(indice_verde_santiago)
dtsantiago<-as.data.frame(indice_verde_santiago)
santiago<-summarise(.data=dtsantiago,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(santiago)
str(santiago)