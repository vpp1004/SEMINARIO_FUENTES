library(devtools)
library(greenR)
library(dplyr)
datoscaceres<-get_osm_data(("Cáceres,Extremadura,España"))
indice_verde_caceres<-calculate_green_index(datoscaceres,4326,100)
str(indice_verde_caceres)
dtcaceres<-as.data.frame(indice_verde_caceres)
caceres<-summarise(.data=dtcacere,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
str(caceres)
