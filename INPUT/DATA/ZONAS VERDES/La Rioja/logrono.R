library(devtools)
library(greenR)
library(dplyr)
datoslogrono<-get_osm_data("Logroño,La Rioja,España")
indice_verde_logrono<-calculate_green_index(datoslogrono,4326,100)
str(indice_verde_logrono)
typeof(indice_verde_logrono)
dtlogrono<-as.data.frame(indice_verde_logrono)
logrono<-summarise(.data=dtlogrono,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(logrono)
str(logrono)