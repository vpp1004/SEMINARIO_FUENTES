library(devtools)
library(greenR)
library(dplyr)
datossanjuan<-get_osm_data("San Juan de la Rambla,Santa Cruz de Tenerife, Canarias,38420,España")
indice_verde_san_juan<-calculate_green_index(datossanjuan,4326,100)
str(indice_verde_san_juan)
typeof(indice_verde_san_juan)
dtsanjuan<-as.data.frame(indice_verde_san_juan)
sanjuan<-summarise(.data=dtsanjuan,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(sanjuan)
str(sanjuan)
