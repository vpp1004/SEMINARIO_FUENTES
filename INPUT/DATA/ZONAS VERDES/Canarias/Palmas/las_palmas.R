library(devtools)
library(greenR)
library(dplyr)
datoslaspalmas<-get_osm_data("Las Palmas de Gran Canaria,Las Palmas,Canarias,España")
indice_verde_laspalmas<-calculate_green_index(datoslaspalmas,4326,100)
str(indice_verde_laspalmas)
typeof(indice_verde_laspalmas)
dtlaspalmas<-as.data.frame(indice_verde_laspalmas)
laspalmas<-summarise(.data=dtlaspalmas,areasverdes=mean(green_index_green_area,na.rm=TRUE),arboles=mean(green_index_tree,na.rm=TRUE),porcentajeverde=mean(green_index,na.rm=TRUE))
View(laspalmas)
str(laspalmas)